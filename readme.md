# Building an neural network model
This is a code to build a classifier using the neural network model for MNIST handwriting dataset.

The model are written in pytorch.

---

## Environment
* Windows 10
* Python 3.7

```bash
# Install packages
pip install -r requirements.txt
```

---
